import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

export default function AddProfile({ visible, handleAddProfile }) {
  return (
    <View>
      <Text variant="displaySmall">TODO: Make into a modal</Text>
    </View>
  );
}
